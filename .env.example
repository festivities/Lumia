DISCORD_TOKEN=
NVIDIA_API_KEY=
OPENAI_API_KEY=

# Moderation provider: omni-moderation (default) or nemotron.
# Only the API key for the selected provider is required.
MODERATION_PROVIDER=omni-moderation

# Set to true to log live media screening progress, evaluations, and verdicts
VERBOSE=true
